using UnityEngine;

public class vlan : MonoBehaviour
{
    public int vlanId;
    public bool trunking;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        vlanId = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
